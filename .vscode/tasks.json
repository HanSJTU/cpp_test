
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
{
    "version": "0.1.0",
    "tasks": [
        {
            "taskName": "build",  // 任务名
            "command": "make",
            "isShellCommand": true,
            "args": ["-j4"],
            "showOutput":"always",
            "options": {
                "cwd": "${workspaceRoot}"
            }
        }
    ]
}